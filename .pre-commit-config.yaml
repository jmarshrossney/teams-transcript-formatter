repos:
  - repo: local
    hooks:
      - id: just
        name: just
        language: system
        pass_filenames: false
        entry: just lint typecheck test
        stages: [pre-commit]
        verbose: true
