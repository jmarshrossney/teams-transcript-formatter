[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"

[project]
name = "teams-transcript-formatter"
version = "0.2.0"
authors = [{name = "Joe Marsh Rossney", email = "17361029+jmarshrossney@users.noreply.github.com"}]
description = "Human-readable Microsoft Teams meeting transcripts"
readme = "README.md"
requires-python = ">= 3.11"
dependencies = ["pandas"]

[project.scripts]
format-transcripts = "teams_transcript_formatter.main:cli"

[dependency-groups]
dev = [
    "pyright>=1.1.409",
    "pytest>=9.0.3",
    "ruff>=0.15.12",
    "rust-just>=1.50.0",
]
